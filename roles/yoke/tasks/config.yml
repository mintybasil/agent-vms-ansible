---
- name: Ensure data dir exists
  ansible.builtin.file:
    path: "{{ yoke_data_dir }}"
    state: directory
    mode: "0755"

- name: Deploy config file
  ansible.builtin.template:
    src: config.toml.j2
    dest: "{{ yoke_data_dir }}/config.toml"
    mode: "0644"
