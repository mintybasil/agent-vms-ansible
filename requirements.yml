# Ansible Galaxy collections for OpenClaw deployment.
# Install with: ansible-galaxy collection install -r requirements.yml -p collections/

collections:
  - name: community.general
    version: ">=8.0.0"
